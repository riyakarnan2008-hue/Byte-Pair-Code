# Byte-Pair-Code
A Python implementation of Byte Pair Encoding (BPE) from scratch for text tokenization, demonstrating vocabulary creation, pair frequency counting, token merging, and subword generation used in modern NLP and Large Language Models (LLMs).
